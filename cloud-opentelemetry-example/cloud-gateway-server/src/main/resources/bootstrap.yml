spring:
  application:
    name: cloud-gateway-server
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.229:8848
      config:
        file-extension: yml
        prefix: ${spring.application.name}
    gateway:
      discovery:
        locator:
          enabled: true
